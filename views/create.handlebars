{{> navbar}}

<article class="message is-dark">
    <div class="message-header">
        <p>Account Information</p>
    </div>
    <div class="message-body">
        <div class="container">
            <div class="row">
                <div class="col-sm-3"></div>
                <div class="col-sm-6">
                    <form id="edit-player" data-id="{{id}}">

                        <div class="field">
                            <label class="label">First Name</label>
                            <input type="text" class="input  is-black" autocomplete="on" id="firstName"
                                value="{{firstName}}">
                        </div>
                        <div class="field">
                            <label class="label">Last Name</label>
                            <input type="text" class="input  is-black" autocomplete="on" id="lastName"
                                value="{{lastName}}">
                        </div>
                        <div class="field">
                            <label class="label">User Name</label>
                            <input type="text" class="input  is-black" autocomplete="on" id="username"
                                value="{{username}}">
                        </div>
                        <div class="field">
                            <label class="label">Email Address</label>
                            <input class="input  is-black" id="email" autocomplete="on" value="{{email}}">
                        </div>
                        <div class="field">
                            <label class="label">Password</label>
                            <input type="password" class="input  is-black" autocomplete="on" id="password"
                                value="{{password}}">
                        </div>
                        <div class="field">
                            <label class="label">Confirm Password</label>
                            <input type="password" class="input  is-black" autocomplete="on" value="{{password}}">
                        </div>


                    </form>

                    <div class="buttons has-addons is-centered">

                        <button href="" type="submit" class="submit button is-outlined is-dark">Submit</button>
                    </div>

                </div>
            </div>
        </div>
    </div>
</article>
{{> footer}}